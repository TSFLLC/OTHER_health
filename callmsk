BEGIN TRAN;

UPDATE c
SET
    -- Names
    CustomerName = 'Masked Customer',
    FirstName    = 'John',
    LastName     = 'Doe',

    -- Emails
    Email        = CONCAT('user', ABS(CHECKSUM(NEWID())) % 100000, '@example.com'),
    AgentEmail   = CONCAT('agent', ABS(CHECKSUM(NEWID())) % 100000, '@company.com'),
    DestAgentEmail = CONCAT('agent', ABS(CHECKSUM(NEWID())) % 100000, '@company.com'),

    -- Phones
    Ani          = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
    Dnis         = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
    Number1      = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
    Number2      = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
    Number3      = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
    AttendanceLine = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
    [Ardent.callbackNumber] = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
    [Studio.patientAni]     = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),

    -- IDs
    ContactId        = ABS(CHECKSUM(NEWID())) % 100000000,
    [Studio.patientId] = ABS(CHECKSUM(NEWID())) % 100000000,

    -- Address
    Street = '123 Masked St',
    City   = 'MaskedCity',
    State  = 'XX',
    Zip    = '00000',

    -- Free text
    Notes = 'Masked',
    [Studio.intentTranscript] = 'Masked',
    CallSurveyResult = 'Masked'

FROM [S_cdpdynamics].[CalllogReport] c;

COMMIT;
++++++++++++++++++++++/////triger================================

  
  CREATE OR ALTER TRIGGER trg_CalllogReport_MaskCritical
ON [S_cdpdynamics].[CalllogReport]
AFTER INSERT
AS
BEGIN
    SET NOCOUNT ON;

    UPDATE c
    SET
        CustomerName = 'Masked Customer',
        FirstName    = 'John',
        LastName     = 'Doe',

        Email        = CONCAT('user', ABS(CHECKSUM(NEWID())) % 100000, '@example.com'),
        AgentEmail   = CONCAT('agent', ABS(CHECKSUM(NEWID())) % 100000, '@company.com'),
        DestAgentEmail = CONCAT('agent', ABS(CHECKSUM(NEWID())) % 100000, '@company.com'),

        Ani          = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
        Dnis         = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
        Number1      = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
        Number2      = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
        Number3      = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
        AttendanceLine = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
        [Ardent.callbackNumber] = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),
        [Studio.patientAni]     = CONCAT('555', RIGHT(ABS(CHECKSUM(NEWID())), 7)),

        ContactId        = ABS(CHECKSUM(NEWID())) % 100000000,
        [Studio.patientId] = ABS(CHECKSUM(NEWID())) % 100000000,

        Street = '123 Masked St',
        City   = 'MaskedCity',
        State  = 'XX',
        Zip    = '00000',

        Notes = 'Masked',
        [Studio.intentTranscript] = 'Masked',
        CallSurveyResult = 'Masked'

    FROM [S_cdpdynamics].[CalllogReport] c
    INNER JOIN inserted i
        ON c.CallId = i.CallId;   -- assumes CallId exists for correlation
END;
GO

  
